function colorize() {
    // let elements = document.querySelectorAll(`tr:nth-child(2n) td`);

    // elements.forEach(element => {
    //     element.style.backgroundColor = `Teal`;
    // });

    let elements = document.getElementById(`id`).nodeValue;

    let elementLast = elements[elements.length - 1];
    console.log(elements);

//     let newElements = Array.from(elements);

//   for (let index = 1; index < newElements.length; index++) {
  
//     if (index % 2 != 0) {
//         newElements[index].style.backgroundColor = `Teal`;
//     }

//   }
}